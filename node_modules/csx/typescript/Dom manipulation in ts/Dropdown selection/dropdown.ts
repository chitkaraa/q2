const select = document.getElementById("countrySelect") as HTMLSelectElement;
const para = document.getElementById("selectedCountry") as HTMLSpanElement;

function dropdownSelection(): void{
    para.textContent = select.value;
}

select.addEventListener("change",dropdownSelection);